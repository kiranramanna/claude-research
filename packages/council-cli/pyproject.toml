[project]
name = "council-cli"
version = "0.1.0"
description = "Local multi-model council deliberation via CLI tools (Gemini CLI, Codex CLI, Claude Code)"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "user contributors" },
]
keywords = ["llm", "council", "multi-model", "cli", "gemini", "codex", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "pydantic>=2.0",
]

[project.urls]
Homepage = "https://github.com/user/council-cli"
Repository = "https://github.com/user/council-cli"
Issues = "https://github.com/user/council-cli/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/council_cli"]
