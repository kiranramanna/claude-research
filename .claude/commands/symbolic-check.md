---
description: 'Use when you need to symbolically verify a SELF-AUTHORED algebra step,
  derivative, limit, comparative-static sign, or closed-form identity using a CAS
  (sympy) — proving or refuting it, not just stress-testing. R2 of the verification
  spectrum: unlike /numerical-check (which only falsifies), symbolic verification
  can positively PROVE a manipulation. Triggers: /symbolic-check, ''verify this algebra'',
  ''is this derivative right'', ''check the sign of a comparative static'', ''does
  this closed form equal the original'', ''verify the limit''. NOT for a full formal
  theorem (use /lean-check), a probabilistic/distributional claim over a parameter
  space (use /numerical-check), or empirical replication (use /cross-language-check).'
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/symbolic-check/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/symbolic-check/`.
