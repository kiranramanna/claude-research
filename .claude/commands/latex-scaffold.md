---
description: Use when you need to convert a Markdown draft into a buildable LaTeX
  project.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/latex-scaffold/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/latex-scaffold/`.
