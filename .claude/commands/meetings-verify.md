---
description: Verify that the meeting-transcribe pipeline is properly set up — whisper/ffmpeg/uv
  binaries, models, package venv, watcher, and folders. Use when the user says "is
  transcription working", "check my meeting setup", "verify minutes", "why isn't transcription
  working", "meeting health check".
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/meetings-verify/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/meetings-verify/`.
