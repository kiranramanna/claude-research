---
description: Use when you need to run the end-of-session checklist (uncommitted changes,
  focus update, project sync). Auto-detects research projects and adds atlas consistency
  checks.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/session-close/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/session-close/`.
