---
description: Use when you need to prune duplicates and merge overlapping entries in
  MEMORY.md files.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/memory-cleanup/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/memory-cleanup/`.
