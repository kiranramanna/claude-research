---
description: 'Use when you need content-aware file reorganisation or deduplication
  driven by a LOCAL model (Ollama on the Mac Mini) that actually reads each file''s
  contents, not just its name — proposes a folder structure or finds exact/near duplicates,
  always dry-runs first, and executes only on approval with a full undo. Triggers:
  ''reorganise this folder'', ''tidy up Downloads with the local model'', ''reorg'',
  ''find duplicate files'', ''dedup this folder'', ''sort these files by reading them''.'
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/reorg/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/reorg/`.
