---
description: Fork an existing paper into a concurrent second-venue submission variant
  with policy checks, separate Overleaf ownership, format conversion, budget refit,
  QA, and Atlas/Vault writeback. Use when submitting the same work concurrently to
  another permitted venue. Not for preprints, ordinary retargeting, or camera-ready
  work.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/venue-fork/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/venue-fork/`.
