---
description: 'Use when you need to numerically stress-test / falsify a SELF-AUTHORED
  mathematical claim — monotonicity, threshold, comparative-static, inequality, closed-form,
  or limit — by Monte-Carlo/sweep over its parameter space. Finds a counterexample
  (definitive falsification) or supports the claim with a large-N no-counterexample
  sweep, with artifact-avoidance discipline (dense sampling, interior grid, noise-aware
  tolerance, characterize violators). R1 of the verification spectrum; the empirical
  arm of mark-unverified for self-authored math. Triggers: numerical-check, ''stress-test
  my conjecture'', ''find a counterexample to'', ''is this monotone/threshold true''.
  NOT for algebra identities (use symbolic-check), machine-proving a lemma (use lean-check),
  or replicating an empirical result (use cross-language-check).'
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/numerical-check/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/numerical-check/`.
