---
description: Use when you need to audit the vault wiki (~/vault/concepts/) for fragmentation,
  missing tags, write-only concepts, and draft/anatomy conformance. Read-only — produces
  a markdown report at /tmp/wiki-curate-report.md. Companion to /wiki-grow (which
  writes) and /wiki-merge (which fixes overlap clusters).
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/wiki-curate/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/wiki-curate/`.
