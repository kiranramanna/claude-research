---
description: Guide for creating MCP servers that enable LLMs to interact with external
  services through well-designed tools. Use when building MCP servers to integrate
  external APIs or services, whether in Python (FastMCP) or Node/TypeScript (MCP SDK).
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/mcp-builder/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/mcp-builder/`.
