---
description: Use when you need Python environment management with uv (install, create
  venv, manage deps).
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/python-env/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/python-env/`.
