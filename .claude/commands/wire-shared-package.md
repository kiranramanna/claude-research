---
description: Use when you need to wire a shared Python package as an editable dependency
  across projects.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/wire-shared-package/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/wire-shared-package/`.
