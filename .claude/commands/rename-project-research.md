---
description: Use when you need to rename a research project across all systems (local
  directory, atlas, vault, Overleaf, git).
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/rename-project-research/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/rename-project-research/`.
