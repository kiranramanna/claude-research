---
description: Use when you need to assemble, anonymize, or audit a replication package.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/replication-package/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/replication-package/`.
