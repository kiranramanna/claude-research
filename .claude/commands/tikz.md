---
description: Use when labels overlap arrows in a TikZ figure, text sits on boxes,
  or arrows cross each other. Audits and fixes residual TikZ visual collisions in
  any .tex file using mathematical gap calculations and Bézier depth formulas — no
  eyeballing. Downstream repair tool; the upstream defense is generating TikZ safely
  via /beamer-deck and tikz_rules.md.
---

# Shared skill adapter

Read and follow `~/.claude/shared-skills/tikz/SKILL.md`. Resolve bundled resources relative to `~/.claude/shared-skills/tikz/`.
